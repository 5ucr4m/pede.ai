import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({
  input: {
    flex: 1,
    height: 48,
    borderColor: "#ccc",
    borderBottomWidth: 1,
    paddingHorizontal: 8,
    fontSize: 16,
    marginTop: 4,
    marginBottom: 16,
  },
});
